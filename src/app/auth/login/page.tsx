import { LoginForm } from "@/features/auth/ui/login/login-form/login-form"

export default function LoginPage() {
  return (
    <div className={"flex h-full w-full items-center justify-center"}>
      <LoginForm />
    </div>
  )
}
